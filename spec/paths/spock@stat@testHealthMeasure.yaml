get:
  parameters:
  - name: startDate
    in: query
    description: 开始时间
    required: true
    schema:
      type: integer
  - name: endDate
    in: query
    description: 结束时间
    required: true
    schema:
      type: integer
  - name: productNames
    in: query
    description: 项目名称
    required: false
    schema:
      type: array
      items:
        type: string
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - spock
  summary: 获取所选时间段内的测试健康度
  responses:
    '200':
      description: 测试健康度
      content:
        application/json:
          schema:
            type: object
            properties: 
              totalSuccess: 
                type: integer
                example: 1000
              totalFailure: 
                type: integer
                example: 10