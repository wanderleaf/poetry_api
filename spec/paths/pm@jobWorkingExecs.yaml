parameters:
  - name: projectId
    in: query
    description: 产品Id
    required: true
    schema:
      type: integer
get:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - ProjectManager
  summary: 列出正在执行的任务工作列表 (server push)
  responses:
    '200':
      description: 获取成功
      content:
        text/event-stream:
          type: array
          schema:
            $ref: '#/components/schemas/JobExecList'