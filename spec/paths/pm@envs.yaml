parameters:
  - name: productId
    in: query
    description: 产品ID
    required: true
    schema:
      type: integer
post:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - ProductManager
  summary: 添加产品环境
  requestBody:
    description: 添加产品环境请求
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/Env"
  responses:
    '200':
      description: 添加成功
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Env'