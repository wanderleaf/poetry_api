parameters:
  - name: pipeLineId
    in: path
    description: 工作流Id
    required: true
    schema:
      type: integer
post:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - ProjectManager
  summary: 开始一个工作流任务
  requestBody:
    description: 工作流任务参数，包含代码库信息
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/PipelineExecRequest"
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/PipelineExec'