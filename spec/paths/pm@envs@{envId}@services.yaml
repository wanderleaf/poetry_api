parameters:
  - name: envId
    in: path
    description: 环境ID
    required: true
    schema:
      type: integer
get:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - ProjectManager
  summary: 获取某环境下服务列表
  description: artifact只显示在当前环境构建的应用包
  responses:
    '200':
      description: "获取成功"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/EnvServiceList"